import type { QuestionItem } from "./types";
export function QuestionsSidebar({ questions, onCourse, onUnanswered, onInfo }: { questions: QuestionItem[]; onCourse: (c: string) => void; onUnanswered: () => void; onInfo: (m: string) => void }) {
  return <aside className="space-y-3"><div className="rounded-2xl border bg-white p-4"><h3 className="font-bold">Cómo hacer una buena pregunta</h3><ul className="mt-2 list-decimal pl-4 text-sm text-slate-600"><li>Sé claro y específico.</li><li>Incluye contexto.</li><li>Agrega adjuntos.</li></ul><button onClick={()=>onInfo("Guía disponible próximamente.")} className="mt-2 text-sm font-semibold text-indigo-600">Ver guía completa</button></div><div className="rounded-2xl border bg-white p-4"><h3 className="font-bold">Preguntas destacadas</h3>{questions.slice(0,3).map(q=><p key={q.id} className="mt-2 text-sm">{q.title}</p>)}</div><div className="rounded-2xl border bg-white p-4"><button onClick={onUnanswered} className="w-full rounded-lg border px-3 py-2 text-sm">Responder ahora</button><div className="mt-2 flex flex-wrap gap-1">{["Matemática","Estadística","Programación","Física","Inglés"].map(c=><button key={c} onClick={()=>onCourse(c)} className="rounded bg-slate-100 px-2 py-1 text-xs">{c}</button>)}</div></div></aside>;
}
