import type { NoteItem } from "./types";

export { NOTE_COURSES, NOTE_MATERIAL_TYPES, NOTE_FILE_TYPES } from "./types";

// Reference seed kept for visual layout only. The Apuntes module now loads
// real data from the backend via /api/apuntes.
export const noteSeed: NoteItem[] = [];
