import type { NoteItem } from "./types";

export function NotesSidebar({ notes, onCourseClick, onNoteClick, onGuide }: { notes: NoteItem[]; onCourseClick: (course: string) => void; onNoteClick: (id: string) => void; onGuide: () => void }) {
  const topCourses = ["Matemática", "Estadística", "Física", "Programación", "Historia"].map((course) => ({ course, count: notes.filter((n) => n.course === course).length }));
  return <aside className="space-y-4"><div className="rounded-2xl border bg-white p-4"><h3 className="font-semibold">Cómo compartir un buen apunte</h3><ol className="mt-2 list-decimal space-y-1 pl-5 text-xs text-slate-600"><li>Sube material propio o con permiso del autor.</li><li>Añade una descripción clara y etiquetas.</li><li>Verifica que el contenido sea legible.</li><li>Usa ejemplos o resúmenes para ayudar.</li><li>Respeta las normas de la comunidad.</li></ol><button className="mt-2 text-sm text-indigo-700" onClick={onGuide}>Ver guía completa</button></div><div className="rounded-2xl border bg-white p-4"><h3 className="font-semibold">Apuntes destacados</h3>{notes.slice().sort((a, b) => b.rating.average - a.rating.average).slice(0, 3).map((n) => <button key={n.id} className="mt-2 block text-left text-sm" onClick={() => onNoteClick(n.id)}>{n.title} — {n.rating.average.toFixed(1)}</button>)}</div><div className="rounded-2xl border bg-white p-4"><h3 className="font-semibold">Cursos con más apuntes</h3>{topCourses.map((item) => <button key={item.course} className="mt-2 block w-full text-left" onClick={() => onCourseClick(item.course)}><p className="text-xs">{item.course}</p><div className="mt-1 h-2 rounded bg-slate-100"><div className="h-2 rounded bg-indigo-500" style={{ width: `${Math.max(8, item.count * 20)}%` }} /></div></button>)}</div><div className="rounded-2xl border bg-white p-4"><h3 className="font-semibold">Top colaboradores</h3><ul className="mt-2 space-y-1 text-sm text-slate-700">{["María Fernández", "Diego Morales", "Andrés López", "Sofía Díaz", "Mateo Vargas"].map((p, idx) => <li key={p}>{idx + 1}. {p}</li>)}</ul></div><div className="rounded-2xl border bg-white p-4"><h3 className="font-semibold">Apuntes recientes</h3><ul className="mt-2 space-y-1 text-xs text-slate-700"><li>Fórmulas de Trigonometría — hace 1 hora</li><li>Guía de Derivadas — hace 3 horas</li><li>Teoremas de Series — hace 5 horas</li><li>Algoritmos de Ordenamiento — hace 6 horas</li></ul></div></aside>;
}
