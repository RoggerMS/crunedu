import type { Conversation } from "@/modules/conversar/types";

export function ConversarFinishedMaterials({ materials }: { materials: Conversation["materials"] }) {
  return <article className="rounded-3xl border border-slate-200 bg-white p-5 shadow-soft"><h2 className="text-lg font-bold text-slate-900">Materiales compartidos</h2>{materials.length ? <div className="mt-3 space-y-3">{materials.map((material) => <div key={material.id} className="rounded-2xl border border-slate-200 bg-slate-50 p-3"><p className="font-semibold text-slate-900">{material.title}</p><p className="text-sm text-slate-600">{material.type.toUpperCase()} {material.size ? `· ${material.size}` : ""}</p><div className="mt-2 flex flex-wrap gap-2"><button type="button" className="rounded-lg bg-slate-900 px-3 py-1.5 text-xs font-semibold text-white">Ver</button><button type="button" className="rounded-lg bg-slate-200 px-3 py-1.5 text-xs font-semibold text-slate-700">Guardar</button>{material.url ? <button type="button" className="rounded-lg bg-slate-200 px-3 py-1.5 text-xs font-semibold text-slate-700">Descargar</button> : null}</div></div>)}</div> : <p className="mt-3 text-sm text-slate-600">No se compartieron materiales en esta conversación.</p>}</article>;
}
