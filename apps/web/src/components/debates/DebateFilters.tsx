import { DebateMode, DebateSort, DebateStatus, DebateTime } from "./types";

export function DebateFilters({ mode, time, status, category, sortBy, categories, onTime, onStatus, onCategory, onSort }: { mode: DebateMode; time: DebateTime; status: "todos" | DebateStatus; category: string; sortBy: DebateSort; categories: string[]; onTime: (v: DebateTime) => void; onStatus: (v: "todos" | DebateStatus) => void; onCategory: (v: string) => void; onSort: (v: DebateSort) => void }) {
  return <div className="space-y-2"><div className="flex gap-2 overflow-x-auto">{([{ k: "esta_semana", l: "Esta semana" }, { k: "semana_actual", l: "Semana actual" }, { k: "todos", l: "Todos" }] as const).map((item) => <button key={item.k} onClick={() => onTime(item.k)} className={`rounded-full px-3 py-1 text-sm ${time === item.k ? "bg-indigo-600 text-white" : "bg-slate-100"}`}>{item.l}</button>)}</div><div className="flex gap-2 overflow-x-auto"><button onClick={() => onCategory("todos")} className={`rounded-full px-3 py-1 text-sm ${category === "todos" ? "bg-indigo-100 text-indigo-700" : "bg-slate-100"}`}>{mode === "academicos" ? "Todos los cursos" : "Todos los temas"}</button>{categories.map((c) => <button key={c} onClick={() => onCategory(c)} className={`rounded-full px-3 py-1 text-sm ${category === c ? "bg-indigo-100 text-indigo-700" : "bg-slate-100"}`}>{c}</button>)}</div><div className="flex gap-2 overflow-x-auto"><button onClick={() => onSort("mas_comentados")} className={`rounded-full px-3 py-1 text-sm ${sortBy === "mas_comentados" ? "bg-slate-900 text-white" : "bg-slate-100"}`}>Más comentados</button><button onClick={() => onSort("mas_recientes")} className={`rounded-full px-3 py-1 text-sm ${sortBy === "mas_recientes" ? "bg-slate-900 text-white" : "bg-slate-100"}`}>Más recientes</button><button onClick={() => onSort("participacion")} className={`rounded-full px-3 py-1 text-sm ${sortBy === "participacion" ? "bg-slate-900 text-white" : "bg-slate-100"}`}>Participación</button>{mode === "academicos" && ([{k:"sin_resolver",l:"Sin resolver"},{k:"en_curso",l:"En curso"},{k:"cerrado",l:"Cerrados"},{k:"todos",l:"Todos"}] as const).map((item)=><button key={item.k} onClick={()=>onStatus(item.k)} className={`rounded-full px-3 py-1 text-sm ${status===item.k?"bg-emerald-100 text-emerald-700":"bg-slate-100"}`}>{item.l}</button>)}</div></div>;
}
