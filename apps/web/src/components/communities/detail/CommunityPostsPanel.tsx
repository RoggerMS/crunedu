type Post = { id: number; title: string; content: string };

export function CommunityPostsPanel({ posts, onCreatePost }: { posts: Post[]; onCreatePost: () => void }) {
  return <section className="rounded-2xl border border-slate-200 bg-white p-5"><div className="mb-4 flex items-center justify-between"><h2 className="text-lg font-bold">Publicaciones recientes</h2><select className="rounded-lg border border-slate-300 px-2 py-1 text-sm"><option>Más recientes</option><option>Más comentadas</option></select></div>{posts.length ? <div className="space-y-3">{posts.map((post) => <article key={post.id} className="rounded-xl border border-slate-200 p-4"><h3 className="font-semibold text-slate-900">{post.title || "Sin título"}</h3><p className="mt-1 text-sm text-slate-600">{post.content}</p></article>)}</div> : <div className="rounded-2xl border border-dashed border-slate-300 p-8 text-center"><h3 className="font-bold text-slate-900">Aún no hay publicaciones</h3><p className="mt-1 text-sm text-slate-600">Sé el primero en compartir algo con esta comunidad.</p><button onClick={onCreatePost} className="mt-4 rounded-xl bg-indigo-600 px-4 py-2 text-sm font-semibold text-white">Crear primera publicación</button></div>}</section>;
}
