type Member = { id: number; name: string; avatarUrl?: string | null; status?: string };

export function CommunityMembersCard({ count, creatorName, members = [] }: { count: number; creatorName: string; members?: Member[] }) {
  const list = members.length ? members.slice(0, 4) : [{ id: 1, name: creatorName, status: "En línea" }];
  const overflow = Math.max(count - list.length, 0);
  return <aside className="rounded-2xl border border-slate-200 bg-white p-4"><div className="mb-2 flex items-center justify-between"><h3 className="font-semibold">Miembros</h3><button className="text-sm font-semibold text-indigo-700">Ver todos</button></div><p className="text-sm text-slate-600">{count} {count === 1 ? "miembro" : "miembros"}</p><div className="mt-3 flex items-center gap-2">{list.map((member) => <div key={member.id} className="h-9 w-9 overflow-hidden rounded-full bg-indigo-100 text-xs font-bold text-indigo-700">{member.avatarUrl ? <img src={member.avatarUrl} alt={member.name} className="h-full w-full object-cover" /> : <div className="flex h-full w-full items-center justify-center">{member.name.charAt(0).toUpperCase()}</div>}</div>)}{overflow ? <span className="rounded-full border border-slate-200 px-2 py-1 text-xs text-slate-600">+{overflow}</span> : null}</div><ul className="mt-3 space-y-2 text-sm">{list.map((member) => <li key={`row-${member.id}`} className="flex items-center justify-between"><span className="font-medium text-slate-700">{member.name}</span><span className="text-xs text-slate-500">{member.status ?? "Hace poco"}</span></li>)}</ul></aside>;
}
