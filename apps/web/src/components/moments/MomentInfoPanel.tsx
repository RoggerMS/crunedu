import Link from "next/link";
import type { MomentItem } from "./types";

export function MomentInfoPanel({ moment, onBoost, onPass, onComment, onSave, onShare }: { moment: MomentItem; onBoost: ()=>void; onPass: ()=>void; onComment: ()=>void; onSave: ()=>void; onShare: ()=>void }) {
  return <div className="space-y-4 rounded-3xl border border-slate-200 bg-white p-5"><h2 className="text-2xl font-black">{moment.title}</h2><p className="text-sm text-slate-600">{moment.description}</p><div className="text-sm text-slate-600"><p>{moment.location}</p><p>Expira en 23 h</p></div><div className="flex flex-wrap gap-2">{moment.tags.map((tag)=><span className="rounded-full bg-slate-100 px-3 py-1 text-xs" key={tag}>#{tag}</span>)}</div><div className="grid grid-cols-3 gap-2 text-sm"><p>{moment.stats.boosts} impulsos</p><p>{moment.stats.confirmations} confirmaciones</p><p>{moment.stats.comments} comentarios</p></div><div className="grid grid-cols-2 gap-2"><button onClick={onBoost} className="rounded-xl bg-indigo-600 px-4 py-3 font-semibold text-white">Impulsar</button><button onClick={onPass} className="rounded-xl border border-slate-300 px-4 py-3 font-semibold">Pasar</button></div><div className="flex flex-wrap gap-2 text-sm"><button onClick={onComment} className="rounded-xl border px-3 py-1">Comentar</button><button onClick={onSave} className="rounded-xl border px-3 py-1">Guardar</button><button onClick={onShare} className="rounded-xl border px-3 py-1">Compartir</button><Link href={`/app/momentos/${moment.id}`} className="rounded-xl border px-3 py-1">Ver detalles</Link></div></div>;
}
