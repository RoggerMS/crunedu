import Link from "next/link";
import type { MomentItem } from "./types";
export function MomentsSavedView({ moments, onRemove }: { moments: MomentItem[]; onRemove: (id: string)=>void }) { if (!moments.length) return <div className="rounded-2xl border border-dashed bg-white p-8 text-center"><p>No tienes momentos guardados.</p><Link href="/app/momentos" className="mt-3 inline-block rounded-xl bg-indigo-600 px-3 py-2 text-white">Volver a Momentos</Link></div>; return <div className="space-y-2">{moments.map((m)=><article key={m.id} className="flex items-center justify-between rounded-2xl border bg-white p-3"><div><p className="font-semibold">{m.title}</p><p className="text-xs text-slate-600">{m.type}</p></div><div className="flex gap-2"><button onClick={()=>onRemove(m.id)} className="rounded-xl border px-3 py-1">Quitar</button><Link href={`/app/momentos/${m.id}`} className="rounded-xl border px-3 py-1">Ver</Link></div></article>)}</div>; }
