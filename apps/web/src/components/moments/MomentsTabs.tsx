import type { MomentView } from "./types";
const tabs: { key: MomentView; label: string }[] = [{ key: "moments", label: "Momentos" }, { key: "news", label: "Noticias" }, { key: "gallery", label: "Galería" }, { key: "saved", label: "Guardados" }, { key: "trends", label: "Tendencias" }];
export function MomentsTabs({ activeView, setActiveView }: { activeView: MomentView; setActiveView: (view: MomentView) => void }) { return <nav className="order-3 -mx-1 flex w-full gap-1 overflow-x-auto px-1 lg:order-none lg:w-auto">{tabs.map((tab)=><button key={tab.key} onClick={()=>setActiveView(tab.key)} className={`shrink-0 rounded-xl px-3 py-2 text-sm font-semibold transition ${activeView===tab.key?"bg-indigo-600 text-white shadow-md shadow-indigo-200":"bg-slate-100 text-slate-700 hover:bg-slate-200"}`}>{tab.label}</button>)}</nav>; }
