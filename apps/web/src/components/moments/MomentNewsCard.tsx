import Image from "next/image";
import type { MomentNewsSummary } from "./types";
export function MomentNewsCard({ item, onOpenMoments }: { item: MomentNewsSummary; onOpenMoments: ()=>void }) { return <article className="grid gap-3 rounded-2xl border bg-white p-3 md:grid-cols-[220px_1fr]"><Image src={item.coverImageUrl ?? "https://picsum.photos/seed/news/440/260"} alt={item.title} width={440} height={260} className="h-40 w-full rounded-xl object-cover"/><div><h3 className="text-lg font-bold">{item.title}</h3><p className="text-sm text-slate-600">{item.summary}</p><div className="mt-2 flex gap-2">{item.tags.map((tag)=><span key={tag} className="rounded-full bg-slate-100 px-2 py-0.5 text-xs">#{tag}</span>)}</div><button onClick={onOpenMoments} className="mt-3 rounded-xl bg-indigo-600 px-3 py-2 text-sm font-semibold text-white">Ver momentos</button></div></article>; }
