"use client";
import { useState } from "react";
import type { MomentComment } from "./types";
export function MomentCommentsDrawer({ open, onClose, comments, onComment }: { open: boolean; onClose: ()=>void; comments: MomentComment[]; onComment: (text: string)=>void }) { const [text, setText] = useState(""); if (!open) return null; return <div className="fixed inset-0 z-30 bg-black/40" onClick={onClose}><div className="absolute bottom-0 left-0 right-0 max-h-[70vh] rounded-t-2xl bg-white p-4" onClick={(e)=>e.stopPropagation()}><h3 className="font-bold">Comentarios</h3><div className="mt-2 space-y-2 overflow-y-auto">{comments.map((c)=><p key={c.id} className="rounded-lg bg-slate-100 p-2 text-sm"><b>{c.author}: </b>{c.content}</p>)}</div><div className="mt-3 flex gap-2"><input value={text} onChange={(e)=>setText(e.target.value)} className="flex-1 rounded-xl border p-2" placeholder="Escribe un comentario"/><button onClick={()=>{ if(text.trim()){ onComment(text.trim()); setText(""); }}} className="rounded-xl bg-indigo-600 px-3 py-2 text-white">Publicar</button></div></div></div>; }
