export function UniversityFilters({ selected, onChange }: { selected: string; onChange: (filter: string) => void }) { const filters = ["Todo", "Trámite", "Convocatoria", "Evento", "Servicio", "Guía", "Urgentes", "Oficiales"]; return <div className="flex flex-wrap gap-2">{filters.map((filter) => <button key={filter} onClick={() => onChange(filter)} className={`rounded-full px-3 py-1 text-xs font-semibold ${selected === filter ? "bg-indigo-600 text-white" : "bg-white border"}`}>{filter}</button>)}</div>; }
